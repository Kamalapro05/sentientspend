# ── required ─────────────────────────────────────────────
streamlit>=1.30
pandas>=2.0
numpy>=1.24
plotly>=5.18
scikit-learn>=1.3
openpyxl>=3.1          # Excel export

# ── recommended (features degrade gracefully without them) ─
thefuzz>=0.20          # fuzzy auto-tagging of bank descriptions
python-Levenshtein     # makes thefuzz ~10x faster
reportlab>=4.0         # PDF reports
requests>=2.31         # live FX + crypto fallback
yfinance>=0.2          # live crypto prices

# ── optional ──────────────────────────────────────────────
pytesseract>=0.3       # receipt OCR (also needs the Tesseract binary:
pillow>=10.0           #   apt install tesseract-ocr  /  brew install tesseract)
SpeechRecognition>=3.10 # voice transaction entry (Google STT)
anthropic>=0.40        # LLM-powered chat answers (needs ANTHROPIC_API_KEY)